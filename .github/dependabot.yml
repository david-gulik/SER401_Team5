version: 2
updates:
  # Keep GitHub Actions (checkout, setup-python, etc.) patched.
  # Python deps are intentionally excluded until the requirements
  # files are restructured (see IVE requirements split work).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
